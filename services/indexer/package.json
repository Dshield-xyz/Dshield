{
  "name": "@dshield/indexer-service",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone indexer for the DShield shielded pool: scans deposit/withdraw events via Stellar RPC, maintains a local commitment/tree store, and serves Merkle paths over HTTP.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "test": "vitest run"
  },
  "dependencies": {
    "@dshield/core": "file:../../packages/core",
    "@noir-lang/noir_js": "1.0.0-beta.9",
    "@stellar/stellar-sdk": "^16.0.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^4.1.9"
  }
}
