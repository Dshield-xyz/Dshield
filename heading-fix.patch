diff --git a/CHANGELOG.md b/CHANGELOG.md
index dd640f2..b17e2da 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -19,3 +19,7 @@ The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
 
 - Updated README.md with detailed build, run, and verification instructions
 - Frontend Merkle tree reconstruction (withdraw + compliance report) now pages through `get_commitments_page` instead of calling the unbounded `get_commitments()` (#57)
+
+### Fixed
+
+- Fixed a heading-order accessibility violation on the landing page (Features section jumped from h1 to h3) and added regression test coverage for the home page in the axe-core suite
diff --git a/frontend/src/app/a11y.spec.tsx b/frontend/src/app/a11y.spec.tsx
index 6139562..0122f88 100644
--- a/frontend/src/app/a11y.spec.tsx
+++ b/frontend/src/app/a11y.spec.tsx
@@ -18,6 +18,13 @@ vi.mock("@/components/WalletProvider", () => ({
 }));
 
 describe("accessibility (axe-core regression guard)", () => {
+  it("home page has no axe violations", async () => {
+    const { default: Home } = await import("./page");
+    const { container } = render(<Home />);
+    const results = await axe(container);
+    expect(results).toHaveNoViolations();
+  });
+
   it("deposit page has no axe violations", async () => {
     const { default: DepositPage } = await import("./deposit/page");
     const { container } = render(<DepositPage />);
diff --git a/frontend/src/app/page.tsx b/frontend/src/app/page.tsx
index eb9504c..be1e475 100644
--- a/frontend/src/app/page.tsx
+++ b/frontend/src/app/page.tsx
@@ -216,6 +216,7 @@ export default function Home() {
 
       {/* Features grid */}
       <section className="mx-auto max-w-5xl px-4 py-12 sm:px-6 sm:py-16">
+        <h2 className="sr-only">Features</h2>
         <div className="grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-4">
           {FEATURES.map(({ icon: Icon, title, description }) => (
             <div key={title} className="scroll-reveal">
